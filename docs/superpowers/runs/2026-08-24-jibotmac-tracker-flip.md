# Run: jibotmac-tracker-flip

Instruction: Execute kata jibot-code#6rzb per TASK-jibot-code#6rzb.md in this worktree: (1) on jibotmac (ssh jibotmac), install the kata client, fix/replace the unhealthy com.jibot.kata-tunnel LaunchAgent (last exit 255), and flip ~/.jilog.toml tracker to kata project jilog — but per Joi's 2026-08-24 ruling, split the digest into a TRACKED run from a second config carrying only non-Hermes readers, and a LOCAL-only tracker=none run carrying the Hermes reader; the two launches MUST use distinct --processed-file and --digest-dir values. (2) Investigate amplifierd WhatsApp-DM session persistence (likely container/gateway-side on cell-jibot, read-only there) and add jilog reader coverage or file a scoped follow-up with findings. Start at medium rung — never light; independent-model (fresheyes) review required before finishing. Repo is NOT marshal-managed: end with the normal finishing flow on branch 6rzb-jibotmac-tracker-flip. Commit redacted copies of machine-local config/launchd artifacts into the branch so the reviewer can see them. Close the kata issue with full evidence, then /finish-worktree.

Stage: done
Rung: heavy (start-floor heavy: hard-trigger — security-sensitive surface [Hermes user text / KATA_AUTH_TOKEN] + production config on jibotmac; BR2 REV1 NOV1 INT1 FC2 = 7; dispatcher floor was medium, evaluator raised). No early exit — hard-trigger-floored.
Lens (mandatory focus in EVERY review pass): "do not restart anything beyond the kata tunnel and the jilog launchd jobs; no reboots; no changes to gateway LaunchAgents; never put secret values or user text in kata issue bodies filed from the tracked run"; "the two launches MUST use distinct --processed-file and --digest-dir values"; "LINE/Telegram/GIDC-email user text must never land in fleet-visible kata issues"
Spec: docs/superpowers/specs/2026-08-24-jibotmac-tracker-flip-design.md
Plan: docs/superpowers/plans/2026-08-24-jibotmac-tracker-flip.md
Agency project: —
Kata: jibot-code#6rzb (pre-existing, already claimed — no new issue filed)

## Scorecards
Pass 1 [spec]: 1B/9S/1C/0R · fixed -/- · velocity = (—→11, escalation no) · judge: pre-floor
  (fresheyes/gpt. BLOCKER "tracked run needs content redaction" REBUTTED in part: Joi's ruling scopes the privacy boundary to Hermes surfaces; claude-code→kata snippets are the fleet-wide baseline since 2026-07-05; boundary is structural (no hermes reader in tracked config). Spec gained an explicit Privacy-boundary section. 8 SUBSTANTIVE adopted: wrapper preflight+timeout, processed-file seeding, transactional cutover+backups+plutil lint, 0600 token plist, REDACTED literal, disclosure-prevention rollback language, restart prohibition; 2 deferred-with-follow-up (jilog fail-loud tracker semantics; hardcoded digest path in issue bodies). COSMETIC fixed.)

Pass 2 [spec]: 1B/7S/0C/0R · fixed 10/10 prior · velocity ↓ (11→8, escalation no) · judge: pre-floor
  (fresheyes/gpt. BLOCKER = same redaction claim re-asserted; rebuttal STRENGTHENED in spec (issue scoped part 1 "cold-implementable" on existing binary; ruling ratifies structural split; fleet precedent). All 7 SUBSTANTIVE adopted: wrapper post-run stderr grep for tracker.create/list_open failures (exit 2), two-stage arming of --create-issues with live-args Stage-1 digest as payload preview, tracked-lane-first cutover ordering with both-artifact restore, timeout harness test w/ stub child+grandchild + env overrides, log-dir mkdir before bootstrap, argument-level isolation gate (not content divergence), tracked digest dir → ~/.amplifier/health making the 0.6.0 hardcoded issue-body path accurate with zero code change.)

Pass 3 [spec]: 1B/2S/3C/0R · fixed 8/8 prior · velocity ↓ (8→6, escalation no) · judge: STOP-VELOCITY
  (independent subagent, correctness/completeness lens. BLOCKER: stderr grep dead — tracing writes stdout; fixed with combined capture + gate c2. SUBSTANTIVE: deterministic create-path canary (gate c3); label-SET launchctl gate (h). 3 COSMETIC fixed. Judge: all fixed adequately, convergence signature (later passes debugging earlier passes' fixes), spec approved.)

Pass 1 [plan]: 4B/12S/0C/0R · fixed -/- · velocity = (—→16, escalation no) · judge: pre-floor
  (fresheyes/gpt. All 16 adopted via full plan rewrite: current jibotmac file bytes embedded as byte sources (kills the snapshot-ordering blocker + enables local semantic zone check via tomllib), gate greps corrected (type-level, comment-stripped, positive assertions), env-sourced token + umask 077 + atomic rename install, PID-recording timeout stub, per-test log dirs + invocation sentinel for gate e, canary dirs-before-bootstrap + RUNID-unique fixture + bootout-gated cleanup, launchctl print "arguments = {" sed blocks, wrapper-defaults-based gate b, two-artifact failure recovery in task 6, committed tracked plist now FINAL ARMED form with Stage-1 strip-at-install.)

Pass 2 [plan]: 3B/11S/1C/0R · fixed 16/16 prior · velocity ↓ (16→15, escalation no) · judge: pre-floor
  (fresheyes/gpt RETRY — attempt 1 died without a review, see Notes. Fixed: bounded preflight in own process group + hang harness test (gate e2), exact-node REDACTED grep, dead diff command removed, tmp+cmp+mv snapshot/seed, idempotent bootstrap w/ bootout prefix + never-delete-loaded-label, asserted harness gates (PASS/FAIL lines, missing pid file = FAIL, executor 300s tool timeout), gate b definition-level only, RUNID persisted to file + RUNID in fixture FILENAME (session-id-unique retries) + failure-path cleanup, gate f split into scratch-ledger dry-run (f1) + poll-to-completion job check (f2), gate g poll-to-completion, gate h three independent commands asserting the diff content, kata search-before-filing, Task 8 final commit + Task 9 close-out steps, v0.2.0 comment corrected. Rebutted: reviewer's speculative `--test` close flag — plan verifies flags via kata close --help and keeps the established --done convention. [CORRECTION at execution: the rebuttal was WRONG — kata close requires typed evidence and has a real `--test` flag; the canary close used `--done --test`, and the plan's close command was corrected. Recorded per roborev job 965.])

Pass 3 [plan]: 0B/3S/5C/0R · fixed 15/15 prior · velocity ↓ (15→8, escalation no) · judge: STOP-VELOCITY
  (independent subagent, implementability lens — empirically verified bash-3.2 set -u/"$@", pgrep ancestor exclusion, flat-fixture glob, fail-loud grep strings, zero-session digest skeleton, RUNID title truncation. 3 SUBSTANTIVE fixed: tolerant arm-step bootout, unconditional canary-dir wipe on any failure, family-scoped gate-h diff. 5 COSMETIC fixed. Judge: all extensions of known fault classes, plan approved.)

Pass 1 [code:branch]: 2B/7S/5C/0R (Stage 1 subagent: 0B/3S/3C ∥ Stage 2 fresheyes/gpt: 2B/4S/2C; plus roborev patrol jobs 964/965 folded into this round) · fixed -/- · velocity = (—→14, escalation no) · judge: pre-floor
  (roborev: gitignore-swallowed jilog-local.toml BLOCKER-class → renamed+tracked; exit-code collision → exit 4; rollback narrowed; preflight diagnostics; digest.rs contract comments; log truncation. Stage 1: plan wrapper block deprecated to pointer; README privacy invariant sentence; verbatim-fx51/list_open/rc7 harness runs; branch-name typos; mktemp/truncation tweaks. Stage 2: fx51 exit 0→5 (design synthesis of roborev-965 vs fresheyes positions); TERM/INT/HUP trap forwarding to child pgroup; timeout-override validation; harness FAIL branches exit nonzero + addendum; rollback glob→find (zsh no-match abort); JST step + "no code changed" text corrected. All fixed in e3d0b0f + ea07898 + this commit; full contract re-proven on jibotmac incl. trap test.)

Pass 2 [code:branch]: 0B/4S/7C/0R (Stage 1 subagent: 0B/1S/3C ∥ Stage 2 fresheyes/gpt: 0B/3S/4C) · fixed 14/14 prior · velocity ↓ (14→11, escalation no) · judge: pre-floor
  (Stage 1 verified every pass-1 fix holds + all three lens classes clean; its SUBSTANTIVE — fx51 carve-out would swallow a list_open parse failure → one-word grep scope fix, re-proven T-lo51→2/T-fx51→5. Stage 2 SUBSTANTIVEs: canary gate expectation updated to exit-5 ending; executable post-hardening harness block added to the plan; rollback scoped via -newer deploy-snapshot anchor so pre-existing digests survive. Cosmetics: mktemp in exit-1 docs, single-shot trap, plan Step 9 + spec §3 superseded-contract pointers, spec JST/docs-only corrections, manifest branch-name note. Stage 1 pass-2 agent ran 6.5h (hung secret-sweep step, self-recovered) — shepherd pinged; result was received and used, nothing redone.)

Pass 3 [code:branch]: 0B/6S/3C/0R (Stage 1 subagent, failure-modes/rollback lens: 0B/4S/3C ∥ Stage 2 fresheyes/gpt: 0B/2S/0C; roborev 967-969 verified fixed-at-HEAD, 971 folded in) · fixed 11/11 prior · velocity ↓ (11→9, escalation no) · judge: STOP-IMPACT (all pass-3 findings fixed and verified in the artifact; all extensions of exhausted fault classes; zero new categories; both stages' privacy/isolation lenses CLEAN; loop closed)
  (Stage 1: canary plist added to every rollback path incl. README full rollback; Step 9/spec rollback prose defers to the README find-newer form; lo51 stub in the re-run harness; anchor-exists abort before digest deletion; idempotent tracked-label bootout; trap test polls for the stub pid file; create-line-only wording. Stage 2: corrected the overstated list-drift claim (jilog silently swallows successful-but-drifted list JSON — unwrap_or empty; recorded on jilog#fx51 as the list-path variant; wrapper classification is defensive for what jilog CAN print); plan harness rebuilt as an ASSERTING harness (per-stub want codes, nonzero on mismatch) and run: HARNESS-PASS.)

## Chunks
- [x] Chunk A (Task 1, repo artifacts) — evaluator 95/100, committed ef9850b. Gates: bash -n OK; plutil -lint OK ×3; `<string>REDACTED</string>` = 1 in both token plists; tracked-TOML positive asserts 1/1/1/1 + hermes|generic 0; tomllib LOCAL-CONFIG-OK.
- [x] Chunk B (Tasks 2–7, jibotmac deploy + verify) — all Agency evaluators clean (95/98/96/92/97).
  - Task 2: drift check TOML-NO-DRIFT + plist deltas = exactly the planned two; snapshots (jilog.toml.orig 1475B, nl plist 1476B, launchctl-list-before 20445B, tmp+cmp+mv); seed SEEDED 1402 lines; WRAPPER-OK/WRAPPER-SYNC/TOML-SYNC; dry-run live-args sessions_scanned=36, signals 0; plist INSTALLED (env token, umask 077, atomic, mode 600, Stage-1 strip); bootstrap OK, arguments = wrapper only.
  - Task 3: GATE-C-PASS (exit 3, child+grandchild PIDs dead), GATE-C2-PASS (exit 2 on stdout marker), GATE-E-PASS (exit 1, sentinel absent), GATE-E2-PASS (hanging kata killed at 5s, exit 1); CLEANED.
  - Task 4: Stage-1 kickstart → run log `2026-08-25T01:36:27Z OK`, last exit 0, digest /Users/jibot/.amplifier/health/learning-digest-2026-08-25.md; payload preview = zero-signal skeleton (36 sessions in window, seeding suppressed history) — nothing private, SAFE TO ARM; gate b tracked half: wrapper defaults greps 1/1.
  - Task 5: canary RUNID 20260825T013913Z (persisted, in fixture filename); precondition 0; canary run created kata jilog#jvd2 (title carries RUNID) — closed with `--test` evidence; CANARY-REMOVED (bootout-gated); ARMED + reload, arguments = wrapper + --create-issues, mode 600. **DISCOVERY:** jilog 0.6.0 KataTracker create-response parse expects `number`, absent in kata v0.15.1 create JSON → server-side create SUCCEEDS but jilog warns (verbatim, backticks included — this exact text is the wrapper filter's reference): ``tracker.create failed: tracker backend: kata create JSON parse: missing field `number` at line 1 column 762`` → wrapper exit 2 (fail-loud working as designed). Arming rationale: issues file correctly server-side; idempotency keys + processed ledger prevent dupes; exit-2 keeps the defect visible until the jilog fix lands (follow-up filed in Task 8). Post-roborev + Stage-2 refinement: the known fx51 pattern is SEPARATED from the exit-2 signal into its own exit 5 (degraded-but-known — nonzero, distinct from real trouble; roborev 965 objected to saturation, fresheyes objected to exit-0 hiding degradation, exit 5 satisfies both); jilog's own nonzero exits map to wrapper exit 4 (no collision with codes 1/2); TERM/INT/HUP trap forwards to the child process group (exit 143); non-integer timeout overrides fall back to defaults. ALSO KNOWN (roborev job 965): while fx51 stands, the digest UNDER-REPORTS — created_issues stays empty and signals carry no #ref backlinks even though the issues exist in kata; the fx51 fix must restore digest/tracker agreement, not just the exit code. Hardened-wrapper harness evidence (final contract, on jibotmac): real create-error stub → exit 2; VERBATIM backticked fx51 line stub → exit 5 + "known create-parse defect … exit 5" log line (Stage-2 review moved this from 0 to 5: nonzero so a degraded night never reads healthy, distinct from 2 so it never masquerades as real trouble); list_open warn stub → exit 2; rc-7 stub → exit 4; clean stub → 0; TIMEOUT_SECS=notanumber tolerated (falls back to default, run completes); TERM-trap test: wrapper killed mid-run → wrapper dead and child process group dead within one poll tick (first attempt of this test raced bash's ≤5 s trap delivery with a 4 s settle — re-run with 10 s settle passed). Deployed wrapper byte-identical to committed after every revision (WRAPPER-SYNC-2/3/4/5). Code-review pass-2 addition: the exit-5 carve-out is scoped to the CREATE line only — a list_open warn carrying the same `missing field \`number\`` string exits 2 (proven: T-lo51 exit 2, T-fx51 exit 5), so a list_open WARN carrying that string can never masquerade as the known defect. [Pass-3 correction: jilog emits `tracker.list_open failed` only on COMMAND failure — a successful `kata list` returning drifted JSON is silently swallowed upstream (`unwrap_or` empty; recurrence dedup quietly degrades, nothing printed for any wrapper to see). The wrapper's classification is defensive for whatever jilog prints; the silent-drift hole is recorded on jilog#fx51.] Final asserting harness (per-stub want-codes, nonzero on mismatch): T-warn 2 / T-fx51 5 / T-lo51 2 / T-rc7 4 / T-ok 0 / T-badtimeout 0 → HARNESS-PASS.
  - Task 6: LOADER-OK + atomic swap + DEPLOYED-EQ-COMMITTED; asserts claude-code 0/hermes 1/none 1/zones 2; local plist reloaded, explicit isolation args in launchctl print; f1 sessions_scanned=14 (scratch ledger); f2 log 102→104, digest in ~/.jilog/digests, exit 0; g spool-ingest exit 0; h label-set diff = exactly `> com.jibot.jilog-nightly-tracked`, 12 gateway PIDs unchanged, TUNNEL-OK, KATA-OK.
  - Task 7: LOCAL-PLIST-SYNC; TRACKED-PLIST-SYNC (modulo token, armed in both). No drift to reconcile.
  - Isolation (gate b, verbatim): tracked = `--digest-dir /Users/jibot/.amplifier/health` + `--processed-file /Users/jibot/.jilog/telemetry/processed-sessions-tracked.txt` (wrapper defaults); local = `--digest-dir /Users/jibot/.jilog/digests` + `--processed-file /Users/jibot/.jilog/telemetry/processed-sessions.txt` (plist args). Pairwise distinct.
- [x] Chunk C (Task 8, follow-ups + final commit): searched first (no existing coverage), filed FOUR follow-ups (plan said three; the canary discovery added one, justified): jilog#1dvk (warn-only tracker failures + processed-state advance), jilog#re4k (hardcoded digest path + UTC/Local date mismatch), jilog#fx51 (kata v0.15.1 create-response missing `number` — the canary discovery; includes the jibotmac exit-2 consequence + bump runbook pointer), jibot-code#41m3 (amplifierd sessions on macazbd unscanned while joimba is active — Part 2 close-out). This kata build has no link command; cross-refs are in the issue bodies.

## Notes
- Dispatcher floored the start at medium; user instruction forbids downgrade to light.
- Branch name: the verbatim instruction above says `6rzb-jibotmac-tracker-flip`; the ACTUAL branch (and worktree) is `6rzb-jibotmac-jilog-tracker-flip` — dispatcher shorthand, all work landed on the actual branch.
- Investigation deltas vs the brief: kata client already installed on jibotmac (v0.15.1, 2026-08-22, hygiene rollout) with working env in ~/.zshrc.local; tunnel currently connected (255s are historical DNS flaps); hermes-export lane exits rc:1 on missing gidc-email state.db (pre-existing, out of scope, noted for close-out); amplifierd sessions persist on macazbd:~/.amplifier/projects (already covered by macazbd config readers but active-mac-gated; joimba active → unscanned) → Part 2 ends as scoped follow-up issue.
- Start-floor evaluator dispatched on the spec draft; verdict: heavy (recorded above).
- Task 5 time gate: plan assumed jibotmac local = JST; ground truth is UTC+6 (Asia/Thimphu-equivalent, `date +%Z` = +06). The gate's underlying predicate — local date == UTC date, so the UTC digest filename and the Local issue-body pointer agree — was verified directly (both 2026-08-25 at execution, local hour 07). Predicate satisfied → canary proceeded; the literal "wait until 09:00" encoding of the JST assumption was corrected, not skipped.
- Plan pass 2 attempt 1: fresheyes died mid-run (codex cache-TTL error) AND the watchdog attached a concurrent session's log/result from shared /tmp/fresheyes-logs (a pxy2-buzz-catchup review — discarded as not ours). Retried once per protocol.
